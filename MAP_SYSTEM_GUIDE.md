# 🗺️ 大型可探索地圖系統說明

## ✅ 已完成功能

### 1. 草地背景圖片載入
- **檔案位置**: `assets/sprites/grass_battle_bg.png`
- **Texture Key**: `grass_bg`
- **載入位置**: `BootScene.ts` preload()

### 2. 大型地圖設定
- **地圖大小**: 5000x5000 像素（從 3200x3200 擴大）
- **Physics World Bounds**: 5000x5000
- **Camera Bounds**: 5000x5000
- **玩家出生位置**: 地圖中心 (2500, 2500)

### 3. 背景平鋪系統
- **使用技術**: Phaser TileSprite
- **平鋪方式**: 自動重複平鋪背景圖片
- **世界座標**: 背景跟著世界座標存在，不是固定 UI
- **攝影機效果**: 玩家移動時，攝影機顯示大型地圖的一部分

### 4. 弱化重複感
- **半透明 Overlay**: 深色半透明層（0x0a0a0a, alpha 0.25）
- **隨機裝飾物**: 
  - 草叢（深綠色小圓）
  - 石頭（灰色橢圓）
  - 地面陰影（深色圓形）
- **裝飾物數量**: 約 200 個（每 500x500 區域 4 個）
- **無碰撞**: 裝飾物純視覺效果，不影響遊戲玩法
- **分布**: 隨機分布在整張 5000x5000 地圖上

### 5. 保留現有功能
✅ 玩家移動系統
✅ 敵人生成系統（從攝影機可視範圍外生成）
✅ 三種自動武器系統
✅ HUD 顯示
✅ 升級系統
✅ 經驗球系統
✅ 所有視覺效果

---

## 📁 修改的檔案

### 1. BootScene.ts
**修改內容**:
```typescript
// 新增草地背景圖片載入
this.load.image('grass_bg', 'assets/sprites/grass_battle_bg.png');
```

### 2. GameScene.ts
**修改內容**:
1. 地圖大小從 3200x3200 改為 5000x5000
2. 使用 TileSprite 平鋪草地背景
3. 新增半透明 overlay
4. 新增 `createMapDecorations()` 方法生成隨機裝飾物

---

## 🎮 遊戲體驗

### 地圖探索
- 玩家可以在 5000x5000 的大型地圖上自由移動
- 攝影機跟隨玩家，顯示周圍區域
- 背景圖片自動平鋪，無縫銜接

### 視覺效果
- 草地背景提供自然的戰鬥環境
- 隨機裝飾物增加地圖細節
- 半透明 overlay 弱化背景重複感
- 暗角效果聚焦玩家視線

### 敵人生成
- 敵人從攝影機可視範圍外的四周生成
- 隨著玩家移動，敵人會從不同方向出現
- 地圖夠大，提供充足的戰鬥空間

---

## 🔧 技術細節

### TileSprite 平鋪
```typescript
const grassBg = this.add.tileSprite(0, 0, MAP_WIDTH, MAP_HEIGHT, 'grass_bg');
grassBg.setOrigin(0, 0);
grassBg.setDepth(-20);
```

### 裝飾物生成
```typescript
// 每 500x500 區域約 4 個裝飾物
const decorationCount = Math.floor((MAP_WIDTH * MAP_HEIGHT) / (500 * 500) * 4);

// 三種裝飾物類型
// 0: 草叢（深綠色小圓）
// 1: 石頭（灰色橢圓）
// 2: 地面陰影（深色圓形）
```

### 深度層級（Depth）
- `-20`: 草地背景（最底層）
- `-15`: 半透明 overlay
- `-10`: 裝飾物
- `-5`: 漂浮粒子
- `0-10`: 遊戲物件（玩家、敵人、投射物）
- `95`: 暗角效果
- `98-100`: HUD UI
- `200+`: 升級面板

---

## 📊 效能優化

### 裝飾物數量控制
- 總數約 200 個（5000x5000 地圖）
- 使用單一 Graphics 物件繪製所有裝飾物
- 不使用 Sprite，避免大量物件開銷

### TileSprite 優勢
- 自動平鋪，無需手動計算
- GPU 加速，效能優異
- 記憶體佔用低

---

## 🎯 未來擴展建議

### 多種地圖主題
可以為不同關卡準備不同背景：
- 森林地圖：`forest_bg.png`
- 墓地地圖：`cemetery_bg.png`
- 雪地地圖：`snow_bg.png`

### 動態裝飾物
- 可以加入動畫效果（草叢搖擺、水面波紋）
- 可以加入互動效果（玩家經過時草叢晃動）

### 地圖邊界視覺
- 可以在地圖邊緣加入圍牆、樹林等視覺元素
- 提示玩家已到達地圖邊界

---

## 🐛 故障排除

### 背景圖片不顯示
1. 確認 `grass_battle_bg.png` 位於 `public/assets/sprites/` 目錄
2. 確認檔案名稱正確（區分大小寫）
3. 檢查瀏覽器控制台是否有載入錯誤

### 背景重複感太強
- 調整 overlay 的 alpha 值（目前 0.25）
- 增加裝飾物數量
- 使用更大的背景圖片

### 效能問題
- 減少裝飾物數量
- 降低地圖大小
- 使用更小的背景圖片

---

## 📱 測試建議

### 桌面測試
```bash
npm run dev
```
- 使用 WASD 移動角色
- 觀察背景平鋪效果
- 檢查裝飾物分布

### 實機測試
```bash
npm run build
npx cap sync android
npx cap run android
```
- 測試觸控搖桿
- 檢查效能表現
- 確認視覺效果

---

**地圖系統已完成！祝您遊戲開發順利！🎉**
